events { worker_connections 1024; }
http { upstream api_backend { server api:4000; } upstream web_frontend { server web:3000; } server { listen 80; location /api/ { proxy_pass http://api_backend; } location / { proxy_pass http://web_frontend; } } }