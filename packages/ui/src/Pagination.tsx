import React from 'react';
export const Pagination: React.FC<{ current: number; total: number; onChange: (p: number) => void }> = ({ current, total, onChange }) => ( <div className='flex items-center justify-center gap-2 pt-4'> <button disabled={current <= 1} onClick={() => onChange(current - 1)} className='px-3 py-1.5 rounded-lg border border-slate-200 text-xs disabled:opacity-40'>السابق</button> <span className='text-xs text-slate-500 font-bold'>صفحة {current} من {total}</span> <button disabled={current >= total} onClick={() => onChange(current + 1)} className='px-3 py-1.5 rounded-lg border border-slate-200 text-xs disabled:opacity-40'>التالي</button> </div> );