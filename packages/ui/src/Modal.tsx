import React from 'react';
export const Modal: React.FC<{ isOpen: boolean; onClose: () => void; title: string; children: React.ReactNode }> = ({ isOpen, onClose, title, children }) => { if (!isOpen) return null; return ( <div className='fixed inset-0 bg-slate-900/60 backdrop-blur-sm flex items-center justify-center p-4 z-50'> <div className='bg-white rounded-2xl max-w-lg w-full p-6 space-y-4 shadow-2xl border border-slate-200'> <div className='flex justify-between items-center border-b border-slate-100 pb-3'> <h3 className='font-bold text-base text-slate-900'>{title}</h3> <button onClick={onClose} className='text-slate-400 hover:text-slate-600 font-bold'>✕</button> </div> {children} </div> </div> ); };