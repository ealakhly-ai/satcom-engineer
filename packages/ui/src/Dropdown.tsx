import React, { useState } from 'react';
export const Dropdown: React.FC<{ label: string; items: Array<{ label: string; onClick: () => void }> }> = ({ label, items }) => { const [open, setOpen] = useState(false); return <div className='relative inline-block text-right'> <button onClick={() => setOpen(!open)} className='px-3 py-1.5 rounded-xl border border-slate-200 text-xs font-bold text-slate-700 hover:bg-slate-50'>{label} ▾</button> {open && <div className='absolute left-0 mt-2 w-40 rounded-xl bg-white border border-slate-200 shadow-lg py-1 z-50'>{items.map((it, idx) => <button key={idx} onClick={() => { it.onClick(); setOpen(false); }} className='block w-full text-right px-4 py-2 text-xs hover:bg-sky-50 text-slate-700'>{it.label}</button>)}</div>} </div>; };