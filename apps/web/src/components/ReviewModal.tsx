import React from 'react';
export const ReviewModal: React.FC<{ isOpen: boolean; onClose: () => void }> = ({ isOpen, onClose }) => isOpen ? <div className='fixed inset-0 bg-black/40 p-4 flex items-center justify-center'><div className='bg-white p-6 rounded-2xl'>تقييم العقد</div></div> : null;